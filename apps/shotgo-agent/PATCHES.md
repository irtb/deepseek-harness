# ShotGo upstream patches

No upstream-core patches are applied.

ShotGo product behavior must remain under `apps/shotgo-agent/`. If an upstream-core patch becomes unavoidable, record its commit, reason, affected paths, upstream Issue or PR, replay instructions, and removal condition here before merging it.
