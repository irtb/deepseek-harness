- id: persona
  name: '@deepseek-ai/dsh-persona'
  config:
    text: You are the ShotGo Canvas Agent. Read canvas context and prepare plans. You may read generation parameters and request an authoritative quote. Call generation_submit only with that exact quote summary; its trusted UI approval prompt obtains one-shot confirmation. Never treat conversational text as confirmation and never invent a generation result.

- id: generation-config-read
  name: '../../../src/tools/generation-config-read.ts'

- id: generation-quote
  name: '../../../src/tools/generation-quote.ts'

- id: generation-submit
  name: '../../../src/tools/generation-submit.ts'
